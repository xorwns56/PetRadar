server {
    listen 80;

    # Let's Encrypt 도메인 소유 확인용 경로.
    # certbot이 여기에 파일을 놓고, Let's Encrypt가 외부에서 접속해 확인한다.
    # 인증서 발급 전에도 열려 있어야 하므로 기본 설정에 둔다
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    include /etc/nginx/nginx-routes.conf;
}
